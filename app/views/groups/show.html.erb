<div >
グループ名: <%= @group.name%>
</div>

<h3>グループメンバー</h3>
<div >
  <% @group.users.each do |user|%>
    <%= link_to user.name, user %><br>
  <% end %>
</div>

<h1>タスク一覧</h1>
<div >
  <% @group.tasks.each do |task|%>
    <%= link_to task.content, edit_task_path(task) %><br>
  <% end %>
</div>

<br><br>
<div>
  <%= link_to 'タスクを作成する', new_task_path(group_id: @group.id)%>
</div>

<br><br>
<%= link_to "グループに招待する", new_group_user_path(group_id: @group.id)%>
<br>
<%= link_to "グループ名を編集する", edit_group_path(@group)%>
<%= link_to "このグループを削除する", group_path(@group), method: :delete%>
<br>
<%= link_to "戻る", current_user%>

<br><br><br><br>
<h3>グループ一覧</h3>
<div >
  <% Group.all.each do |group|%>
    <%= "id: #{group.id} #{group.name} "%><br>
  <% end %>
</div>
